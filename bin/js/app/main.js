// Generated by CoffeeScript 1.4.0
(function() {
  var init, initPaper, px, _ref, _ref1;

  if ((_ref = this.BUGS) == null) {
    this.BUGS = {};
  }

  this.BUGS.devicePixelRatio = (_ref1 = this.devicePixelRatio) != null ? _ref1 : 1;

  this.BUGS.px = px = function(n) {
    return n * BUGS.devicePixelRatio;
  };

  init = function() {
    return initPaper();
  };

  initPaper = function() {
    var $canvas, ch, cw, path;
    if (!!document.createElement('canvas').getContext) {
      $canvas = $('.main canvas');
      cw = $canvas.width();
      ch = $canvas.height();
      $canvas.attr('width', px(cw));
      $canvas.attr('height', px(ch));
      paper.setup($canvas.get(0));
      path = new paper.Path;
      path.strokeColor = '#87CEEB';
      path.strokeWidth = px(10);
      path.moveTo(px(100), px(100));
      path.lineTo(px(200), px(300));
      path.lineTo(px(400), px(200));
      path.lineTo(px(600), px(350));
      path.lineTo(px(700), px(100));
      path.lineTo(px(cw - 100), px(ch - 100));
      path.smooth();
      return paper.view.draw();
    }
  };

  $(function() {
    return init();
  });

}).call(this);
